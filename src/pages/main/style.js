import styled from 'styled-components';
import { primaryColor } from '../../styles/common';

const S = {}

S.Div = styled.div`
  width: 100px;
  height: 100px;
  border: solid 1px black;
  color: ${primaryColor}
`




export default S;