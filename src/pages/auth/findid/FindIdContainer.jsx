import React from "react";
import { Outlet } from "react-router-dom";

const FindIdContainer = () => {
  return (
    <div>
      <h1>Find ID Container</h1>
      <Outlet />
    </div>
  );
};

export default FindIdContainer;
