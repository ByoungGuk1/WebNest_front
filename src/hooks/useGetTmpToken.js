import React, { use, useEffect } from "react";
import { useDispatch, useSelector } from "react-redux";
import { myPayloadType } from "../modules/user";

const useGetTmpToken = () => {
  return;
};

export default useGetTmpToken;
